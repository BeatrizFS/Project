private static void validatePolicyRange(BenefitPackage request) {
    if (request.affected_policy_range == null) {
        throw new WebportalApiUtility_v1.RequestAPIException(
            400,
            'bad_request',
            'affected_policy_range cannot be null.'
        );
    }

    Map<String, Object> policyRangeBody = new Map<String, Object>{
        'affected_policy_range' => request.affected_policy_range
    };

    WebportalApiUtility_v1.runValidateRequiredFields(
        JSON.serialize(policyRangeBody),
        POLICY_RANGE_REQUIRED_FIELDS
    );
}
